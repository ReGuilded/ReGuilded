/**
 * @example new SimpleToggle({ onChange: doSomething, label: "Enable stuff", defaultValue: false })
 * @type {(props: {
 *     onChange: (state: boolean) => void,
 *     defaultValue: boolean?,
 *     isDisabled: boolean?,
 *     label: string?
 * }) => React.Component}
 */
const _ = window.ReGuilded.webpackManager.withClassProperty("input")?.default;

export default _;